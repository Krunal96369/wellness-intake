# Base URL of the backend API.
# Leave empty for local dev: requests go to "/api" and Vite proxies them to the
# backend (see vite.config.ts). For production set this to the deployed API
# origin, e.g. https://wellness-intake-api.onrender.com
VITE_API_BASE_URL=
